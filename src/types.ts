export type ModifyOptions = {
  capitaliseFirstChar?: boolean;
  toUpperCase?: boolean;
  toLowerCase?: boolean;
  snakeCase?: boolean;
  camelCase?: boolean;
  trim?: boolean;
  removeExtraSpaces?: boolean;
};
